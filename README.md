# 数独 · Just Sudoku

纯净、离线、零广告的 Android 数独。

- 9×9 标准数独
- 简单 / 中等 / 困难 / 专家
- 唯一解校验
- 草稿、撤销、重做
- 自动保存
- 统计与三套主题
- 禅模式

Android 版本使用一个轻量原生 WebView 外壳加载内置 `index.html`，无需联网权限。

## APK

每次推送到 `main` 后，GitHub Actions 会自动构建 Debug APK。也可在 Actions 页面手动运行 `Build Android APK`。
